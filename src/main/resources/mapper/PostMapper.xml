<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wanted.project.dao.PostMapper">
    <resultMap id="Post" type="com.wanted.project.model.Post">
        <!--
          WARNING - @mbg.generated
        -->
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="user_id" jdbcType="INTEGER" property="userId" />
        <result column="content" jdbcType="VARCHAR" property="content" />
        <result column="anony" jdbcType="INTEGER" property="anony" />
        <result column="created_at" jdbcType="TIMESTAMP" property="createdAt" />
    </resultMap>

    <insert id="createPost" parameterType="com.wanted.project.model.Post" useGeneratedKeys="true" keyProperty="id">
        insert into post(user_id,content,anony,created_at)
        values (#{userId}, #{content}, #{anony}, #{createdAt});
    </insert>

    <delete id="deleteById" parameterType="com.wanted.project.model.Post">
        delete from post where id=#{id};
    </delete>

    <update id="updatePost" parameterType="com.wanted.project.model.Post">
        update post set content=#{content} where id=#{id};
    </update>

    <select id="findById" resultMap="Post">
        select * from post where `id` = #{id};
    </select>

    <select id="findPostsByUserId" resultMap="Post">
        select * from post where `user_id` = #{userId};
    </select>

    <select id="findAll" resultType="com.wanted.project.model.Post">
        select * from post;
    </select>

</mapper>
